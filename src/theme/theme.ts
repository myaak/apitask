export interface ITheme {
  theme: {
    primaryColor: string;
    secondaryColor: string;
    titleColor: string;
    textColor: string;
  };
}

export const theme = {
  primaryColor: "",
  secondaryColor: "",
  titleColor: "#222222",
  textColor: "#333333"
};
